# Example config for local development / docker compose.
# CHANGE all credentials before any non-local deployment.
bind = "0.0.0.0:3000"
database_url = "mysql://root:philharmonic@mysql/philharmonic"
issuer = "philharmonic"
