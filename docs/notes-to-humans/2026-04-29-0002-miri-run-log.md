# Miri run log

Records when miri was run on which crates, per the mandatory
cadence in CONTRIBUTING.md §10.11.

---

## 2026-04-29 (昭和の日) — overdue catch-up

**Last run before this**: 2026-04-22 (7 days ago).
**Checkpoints missed**: Phase 5B, 6, 7 Tier 1, 8 all landed
without miri, plus the weekly cadence.

| Crate | Result | Tests | Time | Notes |
|---|---|---|---|---|
| `philharmonic-types` | ✅ | 94 passed | 17.7s | Clean. |
| `philharmonic-connector-common` | ✅ | 10 passed | 2.1s | Clean. |
| `philharmonic-connector-client` | ✅ | 2 passed | 9.2s | Clean. Ran with `--no-isolation`. |
| `philharmonic-connector-service` | ✅ | 11 passed | 37.0s | Clean. Ran with `--no-isolation`. |
| `philharmonic-policy` | ✅ | 6 passed | 4945s (~82 min) | Clean. Requires `--no-isolation` (`api_token_vectors` reads test vector files from disk). Crypto paths (AES-GCM, SHA-256, Ed25519, COSE_Sign1) run under miri's no-SIMD reference interpreter — explains the long runtime. |

**All five mandatory crates clean. No UB detected.**

`--no-isolation` flag added to `miri-test.sh` in this session
for convenience.

---

## 2026-04-22 — initial miri setup

Miri introduced via `scripts/miri-test.sh`. Validated Wave 2
crypto (SCK + `pht_` token paths) under miri. Refactored
script shape. See commits `bd8bb4a` and `166a67f`.
