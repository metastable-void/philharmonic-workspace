# Miri run log

Records when miri was run on which crates, per the mandatory
cadence in CONTRIBUTING.md §10.11.

---

## 2026-04-29 (昭和の日) — overdue catch-up

**Last run before this**: 2026-04-22 (7 days ago).
**Checkpoints missed**: Phase 5B, 6, 7 Tier 1, 8 all landed
without miri, plus the weekly cadence.

| Crate | Result | Notes |
|---|---|---|
| `philharmonic-types` | ✅ 94 passed, 0 failed | Clean. |
| `philharmonic-connector-common` | ✅ 10 passed, 0 failed | Clean. |
| `philharmonic-policy` | ⚠️ `api_token_vectors` test fails under default isolation | `open` syscall blocked. Not a UB issue — the test reads vector files from disk. Passes with `--no-isolation` / `MIRIFLAGS=-Zmiri-disable-isolation`. Rerunning. |
| `philharmonic-connector-client` | Pending | |
| `philharmonic-connector-service` | Pending | |

### Next update

Fill in remaining crate results and update the table after the
`--no-isolation` runs complete.

---

## 2026-04-22 — initial miri setup

Miri introduced via `scripts/miri-test.sh`. Validated Wave 2
crypto (SCK + `pht_` token paths) under miri. Refactored
script shape. See commits `bd8bb4a` and `166a67f`.
