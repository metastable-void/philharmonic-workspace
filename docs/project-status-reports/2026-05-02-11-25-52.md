## Overall status

Philharmonic is in active implementation with its core architecture, API layer, connector triangle, reference deployment, and all 25 crates now published; as of 2026-05-02 the end-to-end production path is operational, while remaining post-v1-facing work is tracked in the roadmap's post-v1 dispatch plan.

## Recent work

- Added commit-message code-size metadata via a new `Code-stats` trailer (`46a7d29`).
- Captured deployment findings and fixes in notes, including connector URI rewrite mistakes and a six-item integration failure analysis (`a500500`, `2154cbf`, `1751df0`).
- Stabilized connector routing in the API binary by moving router dispatch outside the normal API middleware stack, bypassing auth where appropriate, extracting realm directly from the path, and fixing upstream URI rewriting (`f1c5466`, `6c814fd`, `2f15d7e`, `980f9cc`, `9114201`).
- Corrected lowerer/executor integration so workflow steps go to the mechanics worker while lowered per-endpoint HTTP requests target the connector router with crypto headers; also ensured lowerer plaintext includes `realm` and consumes the API’s abstract endpoint-config format (`60519c3`, `9a0dcb7`, `0d4328e`).
- Fixed mechanics-worker integration details: executor now sends bearer auth and posts to `/api/v1/mechanics`; documented fail-closed behavior when no worker tokens are configured (`672ffc8`, `c4c3d4f`, `6849e07`).
- Improved API/WebUI behavior: newest-first pagination on list endpoints, `/v1/_meta/` scope-resolution bypass, branding endpoint and configurable brand name, `/v1/whoami`, and login/session fixes around tenant scope propagation (`9467985`, `6a4c9b2`, `870af90`, `3b831fb`, `0837a66`, `5af45ad`).
- Expanded the WebUI substantially: added CRUD pages for endpoints, principals, roles, memberships, and minting authorities; added English/Japanese i18n, 24-hour timestamp formatting, and clearer step-output display (`a39ca09`, `7b36194`, `0e68528`, `68892d4`).
- Reconciled docs with implementation reality across design docs, README, ROADMAP, and workflow authoring guides, including the distinction between connector endpoint configs and low-level mechanics HTTP config, and updated examples to match actual TypeScript/module signatures (`cb1934b`, `193d86e`, `97d08d9`, `7764713`, `ed32d6e`).
- Prepared and recorded patch-release work across published crates after doc/visibility/feature-gate changes, including version bumps and changelog completion (`b9545bf`, `ecbb7c2`, `ef8414d`).
- Reworked docs publishing around mdBook and added CI to build it on every push to `main` (`c995856`, `c516e73`).
- Feature-gated Boa out of the meta-crate/API-server path so the API server and connector binaries no longer link `boa_engine` unnecessarily (`49a66f9`).
- Added bootstrap and key-generation operational tooling: bootstrap seeds the initial operator tenant/principal and all three bins gained signing/SCK/realm key generation subcommands (`9768985`, `6bd1134`).
- Split bin targets out of the meta-crate into `bins/` to isolate the 2.28 GB embed payload to the connector binary, then updated musl/release build scripts accordingly (`811bba1`, `e59204c`, `58f95f5`, `32c86fe`).
- Landed deployment tooling and verification work: Docker Compose deployment, install subcommand, musl static builds, full-pipeline e2e tests, security headers, sensitive-file pre-commit blocking, and audit-driven hardening/fixes (`869e7f5`, `77c10e6`, `e46d40e`, `1a252be`, `39dc53c`, `0c648b9`, `a05f7f5`).
- Earlier in this window, completed Phase 9 bin/service infrastructure: shared server module, mechanics HTTPS support, connector-service binary rework, API binary wiring, embedded WebUI, real lowerer/executor, and in-process/testcontainer e2e coverage (`419e12e`, `c39da59`, `e738bac`, `e258287`, `2c37d91`, `363b4e7`, `471b3ee`, `53cb708`).

## What is next

Per `ROADMAP.md`, the main next work is the post-v1 dispatch plan: embedding datasets, the remaining Phase 7 connector implementations deferred past Golden Week (Tier 2 `philharmonic-connector-impl-email-smtp`, then Tier 3 `philharmonic-connector-impl-llm-anthropic` and `philharmonic-connector-impl-llm-gemini` on or after 2026-05-07), and WebUI/docs follow-through. Operationally, keep the reference deployment running and observe at least a week of incident-free operation.

## Notable concerns or blockers

- The roadmap still calls out remaining operational validation: the reference deployment is operational, but a week-long incident-free run had not yet been completed.
- Recent commits show multiple deployment and integration bugs were found and fixed on 2026-05-01, especially around connector routing, lowerer/executor wiring, auth bypass boundaries, and WebUI/API scope behavior (`2154cbf`, `1751df0`, `f1c5466`, `6c814fd`, `60519c3`).
